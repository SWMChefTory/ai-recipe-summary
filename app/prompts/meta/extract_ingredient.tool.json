[
  {
    "toolSpec": {
      "name": "emit_ingredients",
      "description": "Emit a normalized list of ingredients from the video description",
      "inputSchema": {
        "json": {
          "type": "object",
          "required": ["ingredients"],
          "properties": {
            "ingredients": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["name", "amount", "unit"],
                "properties": {
                  "name": { "type": "string" },
                  "amount": { "type": "number" },
                  "unit": { "type": "string" }
                }
              }
            }
          }
        }
      }
    }
  }
]
